package mt.lexer;

public enum MTTokenType {
    EOF,
    IDENTIFIER,
    KEYWORD,
    INTEGER,
    FLOAT,
    STRING,
    BINARY_SELECTOR,
    ASSIGN,
    RETURN,
    LPAREN,
    RPAREN,
    PERIOD,
    SEMICOLON,
    LBRACKET,
    RBRACKET,
    PIPE,
    COLON
}
